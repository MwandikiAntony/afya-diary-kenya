// backend/utils/sendOtp.js
export default async function sendOtp(phone, otp) {
  console.log(`📱 Sending OTP ${otp} to ${phone}`);
  // integrate with your SMS API (e.g. Twilio, Africa's Talking, Infobip, etc.)
}
